/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['i.pravatar.cc', 'github.com', 'picsum.photos'],
    formats: ['image/avif', 'image/webp'],
  },
}
  
  export default nextConfig;